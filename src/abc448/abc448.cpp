#include<iostream>
#include<vector>
#include<numeric>
#include<algorithm>
#include<unordered_map>
using namespace std;
using ll = long long;

int main() {
    int K, M;
    cin >> K >> M;

    vector<int> c(K), l(K);
    for (int i = 0; i < K; i++) {
        cin >> c[i] >> l[i];
    }

    for (int i = K - 1, i > 0; i--){
        c[i]
    }

    return 0;
}