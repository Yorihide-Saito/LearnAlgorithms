#include<iostream>
#include<string>
#include<vector>
#include<stack>
using namespace std;

int main() {
    int h, w; cin >> h >> w;
    vector<string> map;
    for (int i = 0; i < h; i++) {
        cin >> map[i];
    }

    return 0;
}